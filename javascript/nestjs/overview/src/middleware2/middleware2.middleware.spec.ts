import { Middleware2Middleware } from './middleware2.middleware';

describe('Middleware2Middleware', () => {
  it('should be defined', () => {
    expect(new Middleware2Middleware()).toBeDefined();
  });
});
