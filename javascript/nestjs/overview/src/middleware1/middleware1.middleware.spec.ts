import { Middleware1Middleware } from './middleware1.middleware';

describe('Middleware1Middleware', () => {
  it('should be defined', () => {
    expect(new Middleware1Middleware()).toBeDefined();
  });
});
